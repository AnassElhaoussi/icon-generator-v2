export interface IGeneration {
    prompt: string,
    iconObject: string,
    iconDescription: string,
    color: string,
    style: string
    URLs: string[],
    n: number,
    authorEmail: string
}